<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.app.domain.user.ProfileRepository">

	<insert id="changeProfile" parameterType="com.test.app.domain.user.Profile">
		insert into user_dtl values(
			#{username}, #{nickname}, #{address}, #{gender}
		);
	</insert>
	<insert id="changeProfile" parameterType="com.test.app.domain.user.Profile">
		insert into user_dtl values(
			#{username}, #{nickname}, #{address}, #{gender}
		);
	</insert>
	<insert id="changeProfile" parameterType="com.test.app.domain.user.Profile">
		insert into user_dtl values(
			#{username}, #{nickname}, #{address}, #{gender}
		);
	</insert>

	<select id="checkNickname" parameterType="com.test.app.domain.user.Profile" resultType="Integer">
		select count(nickname) from user_dtl where nickname = #{nickname};
	</select>

</mapper>