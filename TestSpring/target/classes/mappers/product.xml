<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.app.domain.user.ProductRepository">

	<select id="homeList" parameterType="Integer" resultType="com.test.app.domain.user.Product">
		select * from product limit #{number},8;
	</select>
	
	<select id="getProductList" parameterType="Integer" resultType="com.test.app.domain.user.Product">
		select * from product where  product_code = #{product_code};
	</select>


	<insert id="productInsert" parameterType="com.test.app.domain.user.Product">
		insert into product values(
			0,
			#{product_username},
			#{product_phone},
			#{product_title},
			#{product_content},
			#{product_price},
			#{product_delivery},
			#{product_status},
			#{product_exchange},
			#{product_select},
			#{product_img1},
			#{product_img2},
			#{product_img3},
			#{product_img4},
			#{product_img5},
			#{product_img6},
			now()
		);
	</insert>



</mapper>